---
"ghagga-core": minor
---

Add first-class `INCONCLUSIVE` review ledger status to `ReviewStatus` and the exhaustive compile/API surfaces (filter, format, CLI exit code, dashboard badge). Consensus vote summaries remain `NEEDS_HUMAN_REVIEW`; this does not remap them.
