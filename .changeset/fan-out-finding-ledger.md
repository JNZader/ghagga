---
"ghagga-core": patch
---

Fan-out stamps a per-finding ledger after merge: `id`, `lens`, `location`, `ledgerStatus` (`open`), and `evidence`. Fields are optional on `ReviewFinding` so other modes stay compatible. No persistence or refuters yet.
